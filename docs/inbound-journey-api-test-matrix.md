# Inbound journey — API routes, services, and automated tests

> **Canonical journey hub (operator + engineering):** [inbound-journey.md](inbound-journey.md)

This matrix ties the **canonical inbound flow** (see [inbound-grn-debit-credit-note-flows.md](./inbound-grn-debit-credit-note-flows.md) and [mobile/inbound-grn-flow-parity.md](./mobile/inbound-grn-flow-parity.md)) to **Next.js routes**, **server services**, and **automated tests**.

| Journey step | User-facing intent | Primary API routes | Primary service modules | Automated coverage |
|--------------|-------------------|---------------------|-------------------------|-------------------|
| List / search GRNs | Find receipts | `GET /api/inbound/grns` | `inboundGrnsService.listGrnsPaginated` | — |
| GRN snapshot | Detail hub | `GET /api/inbound/grns/[grnId]/details` | Detail bundle loaders | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) (fixture GRN bundle shape) |
| Register draft → operational | Draft IDs become real warehouse GRN | `POST …/register-operational` | `registerOperationalGrnId` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs): 400; happy path once per fixture load |
| Edit OPEN lines | Receipt quantities / audit price | `PATCH …/items/[lineIndex]` | `updateInboundGrnItemRaw` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs): 404 invalid line; **409** when audit closed (manual) |
| Vendor invoice upload | JPG/JPEG/PDF, Zap Storage | `POST …/upload-zap` | `upload-zap` route + DB | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) |
| Close GRN | Freeze lines; Zap rate-diff DN on close when discrepant lines | `POST …/close` | `closeGrn`, `tryAutoGenerateDebitNoteAfterGrnClose` | [inbound-grn-flow.test.mjs](../tests/api/inbound-grn-flow.test.mjs), [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) fixtures `988877111` (DN) / `988877112` (no DN) |
| Mark audit closed | Admin closes audit queue; rate-diff DN when discrepant | `PATCH …/grn` `{ grn_audit_status: "CLOSED" }` | `updateGrnStatus`, `tryAutoGenerateDebitNoteAfterGrnClose` | Manual scenarios below |
| Close GRN UX (web) | OPEN shows action; invoices before confirm | Page + modal | `inboundGrnCloseUi`, `inboundVendorInvoiceUi` | [inbound-journey-ui-lib.test.ts](../tests/unit/inbound-journey-ui-lib.test.ts) |
| Zap debit note lifecycle | Assign **dn_number** (`PATCH` body); CN upload → **`CLOSED`**; Tally CSV optional (`…/export`) | `GET|POST|PATCH …/debit-note`, `POST …/debit-note/cn-copy`, `…/export` | `assignDnNumber`, `uploadCnCopy`, `grnDebitNoteService` (+ routes) | [inbound-grn-flow.test.mjs](../tests/api/inbound-grn-flow.test.mjs), [grn-debit-note-helpers.test.ts](../tests/unit/grn-debit-note-helpers.test.ts) |
| eAutomate DCN queue | Pending debit/credit sync | `GET /api/inbound/pending-debit-credit/notes`, `POST …/[noteId]/decision` | `inboundPendingDebitCreditService` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) (with fixtures) |
| Pending audit queue | Rows awaiting audit close | `GET /api/inbound/pending-audits/grns` | `listPendingAuditGrnsPaginated` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) (+ fixture) |
| Audit admin controls | Admin-only mark audited; line lock after audit | `PATCH /api/inbound/grns/[grnId]`, `PATCH …/items/[lineIndex]` | `updateGrnStatus`, `updateInboundGrnItemRaw` | Manual / planned: see scenarios below |
| Pending invoice collection | Physical invoice receipt | `GET /api/inbound/pending-invoice-collection/grns` | `listPendingInvoiceCollectionGrnsPaginated` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) (+ fixture) |
| Pending accounts | Accounts approve / reject (**admin** only) | `GET /api/inbound/pending-accounts/grns` | `listPendingAccountsGrnsPaginated` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs) (+ fixture) |
| Workflow PATCH on GRN | Audit / invoice / accounts fields; terminal `grn_audit_status` and `accounts_status` (APPROVED/REJECTED) require **admin** | `PATCH /api/inbound/grns/[grnId]` | `updateGrnStatus` (+ `normalizeGrnAuditStatusForPatch`) | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs); [inbound-journey-ui-lib.test.ts](../tests/unit/inbound-journey-ui-lib.test.ts) |
| Inventory booking | SKU → bin after APPROVED | `POST …/receive-inventory` | `receiveIntoInventory` | [inbound-journey-integration.test.mjs](../tests/api/inbound-journey-integration.test.mjs): body validation / 422 |
| Invoice Excel | Accounts export after physical copy **COLLECTED** (web button); **includes Zap DN summary + line sheet when a rate-diff note exists**; API has no `COLLECTED` guard | `GET …/invoice-export` | `buildInvoiceExcel` + route | [inbound-grn-flow.test.mjs](../tests/api/inbound-grn-flow.test.mjs) (data-dependent) |
| List filters / CSV (PO hub) | Inbound PO / pending hubs | Listing pages query strings | — | [inbound-po-grn-pending-ui.test.ts](../tests/unit/inbound-po-grn-pending-ui.test.ts) |

## Deterministic fixtures (local / CI against `TEST_DATABASE_URL`)

Seed file: [tests/fixtures/inbound_journey_fixture.sql](../tests/fixtures/inbound_journey_fixture.sql).

```bash
# After migrations against the same DB Next uses:
psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -f web/tests/fixtures/inbound_journey_fixture.sql
```

Numeric band **988877xxx** avoids collision with typical eAutomate IDs. Constants for tests: [tests/fixtures/inbound_journey_constants.mjs](../tests/fixtures/inbound_journey_constants.mjs).

### Register-operational caveat

Successfully calling `POST …/register-operational` **replaces** the draft `grn_id` with the operational id. Re-run the fixture SQL (or CI job that resets the band) before repeating that happy-path test.

### Audit admin control scenarios (manual / planned automation)

| Scenario | Steps | Expected |
|----------|-------|----------|
| Non-admin mark audited | `PATCH /api/inbound/grns/{id}` with `{ grn_audit_status: "CLOSED" }` as non-admin | **403**; `inbound_grn_logs` row with `log_type = AUDIT_DENIED` |
| Admin mark audited | Same PATCH as admin | **200**; `log_type = AUDIT`; row leaves pending-audit queue |
| Line edit after audit | `PATCH …/items/{lineIndex}` when `grn_audit_status` is terminal | **409**; `log_type = AUDIT_LOCKED` |
| Web Confirm Audit | Admin clicks Mark Audited on `/inbound/pending-audits` | Confirm Audit dialog shown; PATCH only after confirm |

### Accounts admin control scenarios (manual / planned automation)

| Scenario | Steps | Expected |
|----------|-------|----------|
| Non-admin approve accounts | `PATCH /api/inbound/grns/{id}` with `{ accounts_status: "APPROVED" }` as non-admin with `purchase_orders:write` | **403**; `inbound_grn_logs` row with `log_type = ACCOUNTS_DENIED` |
| Non-admin without write | Same PATCH as warehouse/viewer (no `purchase_orders:write`) | **403** (insufficient permissions) |
| Admin approve accounts | Same PATCH as admin | **200**; GRN leaves pending-accounts queue |
| Web Approve/Reject | Admin clicks Approve or Reject on GRN Accounts tab or `/inbound/pending-accounts` | PATCH only when user has **admin** role |

## Related docs

- [inbound-journey.md](./inbound-journey.md) — end-to-end inbound journey (steps, routes, roles)  
- [services/inbound/workflows.md](./services/inbound/workflows.md) — bundle shape and queue tables  
- [inbound-grn-debit-credit-note-flows.md](./inbound-grn-debit-credit-note-flows.md) — Zap DN vs eAutomate DCN state diagrams  
