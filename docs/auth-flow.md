# Auth Flow and RBAC (superseded)

**Use instead:**

- [services/auth/overview.md](services/auth/overview.md) — authentication (JWT + API key) and RBAC
- [services/auth/api.md](services/auth/api.md) — `/api/auth/*` routes

The Mermaid diagram in the old version referenced **Basic/Session** paths that **do not exist** in `server/auth.ts`; the overview doc reflects **Bearer JWT** and **X-API-Key** only.
